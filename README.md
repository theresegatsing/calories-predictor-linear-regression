# calories-predictor-linear-regression
A Python project that implements linear regression from scratch (without libraries like scikit-learn) to predict calories burned based on input features such as distance, speed, age, and weight. The model is trained using gradient descent and tested on unseen data for realistic predictions.
